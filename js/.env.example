# The SDK no longer requires any build-time env.
#
# The SDK launches the AntiBrow kernel directly and gets its license token from
# the server, so there is nothing to configure here.
#
# Runtime configuration lives in code / constructor options instead:
#   new AntiDetectBrowser({ key, server, cacheDir, proxyHost, ... })
